1) Projetar o primeiro nome e o último nome dos atores de sexo feminino;

π first_name, last_name σ gender = 'F' actors

2) Projetar o nome dos filmes com ano superior à 1999;

π name σ year > 1999 movies

3) Projetar o nome do filme e o nome do diretor de cada filme;

π name, first_name, last_name σ movies_directors.director_id = directors.id (π name,director_id σ id=movie_id (movies ⨝ movies_directors) ⨝ directors)

4) Projetar o nome do filme, nome do ator e o papel que cada ator teve no filme para filmes com ranking acima da nota 6;

π name, first_name, last_name, role (σ id = actor_id (π name, actor_id, role ((σ rank > 6 movies) ⨝ roles) ⨝ actors))

5) Projetar o nome do diretor e o número de filmes que cada diretor dirigiu;

π first_name, last_name, somados ((γ director_id; count(movie_id) -> somados (movies_directors)) ⨝ directors)


6) Projetar o gênero e o número de filmes de cada gênero; 

γ genre; count(movie_id) -> qtd_genero (movies_genres)

7) Projetar o gênero, o ranking (nota) médio, mínimo e máximo dos filmes do gênero.

γ genre; avg(movies.rank) -> notaGenero(π movie_id ,rank, genre (movies ⟕ movies_genres)) ⨝ γ genre; min(movies.rank) -> notaMinimo(π movie_id ,rank, genre (movies ⟕ movies_genres)) ⨝ γ genre; max(movies.rank) -> notaMaximo(π movie_id ,rank, genre (movies ⟕ movies_genres))

